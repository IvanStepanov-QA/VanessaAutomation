# Зафиксируйте результаты решения заданий 2 и 3 Блока 1 в виде отдельного МР

Вам нужно зафиксировать в своем репозитории Project25f_NN фиче-файлы, которые у вас получились по результатам решения задач 2 и 3 блока 1 согласно базовому процессу разработки (gitlab-flow):

— клонировать репозиторий

— создать отдельную ветку

— поместить ваш фиче-файл в эту ветку, связать коммит с задачей

— отправить изменения во внешний репозиторий

— создать запрос на слияние (MR), назначить рецензентом (reviewer) в этом запросе Кунташова Александра (@akuntashov)

— пройти код-ревью: после получения замечаний от рецензента, выполнить их исправление до принятия MR’а и повторять цикл до исправления всех замечаний (не пугайтесь, справитесь за 1-2 итерации).

## Ожидаемый результат 

В качестве результата выполнения задания необходимо создать отдельный запрос на слияние (merge request, MR), содержащий фиче-файл с указанными сценариями и приложить ссылку на этот MR в качестве решения на обучающей платформе https://edu.infostart.ru 

(текст задания ниже дублирует текст задания Блока 1 для демонстрации возмоности оформления текста задач)

## 2.1. “Создание нового товара” 

Повторите реализацию сценария, созданного в уроке

## 2.2. Создание новой услуги 

Реализуйте отдельный новый сценарий по созданию новой услуги 

## 2.3. Реализуйте негативный сценарий

При попытке записи элемента справочника Товары без наименования в окно сообщений должно выводиться сообщение об ошибке `Поле “Наименование” не заполнено`.

Для проверки наличия сообщения в окне сообщений используйте шаг

```gherkin
    Тогда в логе сообщений TestClient есть строки:
        | 'Текст сообщения' |
```

## 3.1. Проверка доступности реквизитов в зависимости от значения переключателя “Вид товара”

В форме элемента справочника “Товары” есть такой функционал: если переключатель “Вид” устанавливается в позицию “Услуга”, то реквизиты Артикул, Поставщик и Штрихкод становятся недоступными. Если вернуть переключатель в позицию “Товар”, то эти реквизиты снова становится доступными.

Определите, какие случаи необходимо протестировать и автоматизируйте проверку этих случаев.

Для проверки доступности элементов используйте шаги из Библиотеки шагов:

* `И я жду доступности элемента с именем "ИмяЭлемента" в течение 20 секунд`
* `И я жду недоступности элемента с именем "ИмяЭлемента" в течение 20 секунд`

Для определения имени элементов управления формы используйте Исследователь формы.

